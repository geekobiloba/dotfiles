#
# Windows pre-package run-once
#

Write-Host "Starting Windows pre-package run-once" `
  -ForegroundColor Green

# Prepare scoop
scoop install git aria2
scoop config aria2-warning-enabled false
scoop update
scoop bucket add extras

Write-Host "Windows pre-package run-once is done" `
  -ForegroundColor Green

# vim: ft=ps1:
